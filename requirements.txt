# Frozen from project venv (pip install). Bump versions when you upgrade Python/packages.
# Jupyter: pip install jupyter ipykernel  (or use VS Code / Cursor notebook support)
#
# Optional — only if you run AutoGluon cells in notebooks/04_modeling.ipynb (large download):
#   pip install "autogluon.tabular==1.5.0"

pandas==2.3.3
numpy==2.2.6
scipy==1.16.3
scikit-learn==1.6.1
joblib==1.5.3
matplotlib==3.10.8
seaborn==0.13.2
shap==0.51.0
xgboost==3.1.3
lightgbm==4.6.0
pyarrow==20.0.0
flaml[automl]==2.5.0
